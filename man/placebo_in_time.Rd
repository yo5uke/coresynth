% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scm.R
\name{placebo_in_time}
\alias{placebo_in_time}
\title{In-Time Placebo (Backdating) Test for SCM}
\usage{
placebo_in_time(fit, t0_placebo = NULL)
}
\arguments{
\item{fit}{A sharp \code{coresynth} object from \code{\link[=scm_fit]{scm_fit()}} with
\code{method = "scm"}.}

\item{t0_placebo}{Backdated treatment period as a 1-based position in
\code{fit$times}; must satisfy \verb{2 <= t0_placebo < T_pre}. Default
\code{floor(T_pre / 2)}.}
}
\value{
A list with:
\itemize{
\item \code{t0_placebo}: the backdated treatment period used
\item \code{times}: time values of the pre-treatment window
\item \code{unit_weights}: placebo donor weights
\item \code{Y_treat}, \code{Y_synth}, \code{gap}: series over the pre-treatment window
\item \code{placebo_att}: mean placebo gap over \verb{(t0_placebo, T_pre]}
\item \code{fit_rmspe}: RMSPE over the placebo fitting window \verb{1..t0_placebo}
\item \code{eval_rmspe}: RMSPE over the placebo post window \verb{(t0_placebo, T_pre]}
}
}
\description{
Re-estimates the synthetic control after artificially backdating the
treatment to a pre-treatment period, following Abadie, Diamond &
Hainmueller (2015) and Abadie & Vives-i-Bastida (2022, principle 7:
"out-of-sample validation is key"). Only pre-treatment data enter the
exercise, so the placebo gap after \code{t0_placebo} is uncontaminated by the
actual intervention. A credible design shows no sizable divergence at the
backdated treatment time.
}
\details{
The refit uses the outcomes of periods \verb{1..t0_placebo} as predictors
(the \code{predictors = NULL} default), regardless of how the original fit was
specified, because user-supplied \code{pred()} windows cannot be lagged
automatically (ADH 2015 lag their predictors by hand).
}
\seealso{
\code{\link[=mspe_ratio_pval]{mspe_ratio_pval()}} for in-space placebos, \code{\link[=loo_donors]{loo_donors()}} for
donor-robustness checks.
}
