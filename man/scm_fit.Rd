% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scm_fit.R
\name{scm_fit}
\alias{scm_fit}
\title{Fit a Synthetic Control Method Model}
\usage{
scm_fit(
  formula,
  data,
  method = c("scm", "sdid", "gsc", "mc", "tasc", "si"),
  predictors = NULL,
  covariates = NULL,
  v_selection = c("insample", "oos"),
  donor_mspe_threshold = Inf,
  lambda_pen = NULL,
  v_optim = c("auto", "coord_descent", "bfgs", "multistart"),
  v_window = NULL,
  nu = NULL,
  fixedeff = FALSE,
  ...
)
}
\arguments{
\item{formula}{A \code{Formula} object, e.g. \code{y ~ D | unit + time}.}

\item{data}{A \code{data.frame} in \strong{long} format (one row per unit-time).}

\item{method}{One of \code{"scm"}, \code{"sdid"}, \code{"gsc"}, \code{"mc"}, \code{"tasc"}, \code{"si"}.}

\item{predictors}{A \code{list()} of \code{\link[=pred]{pred()}} specifications that define the
predictor matrix for SCM (see Abadie et al. 2010, S.2.3). Each \code{\link[=pred]{pred()}}
entry aggregates one or more variables over a time window. Pass \code{NULL}
(default) to use all pre-treatment outcome periods as predictors.
Applies to \code{method = "scm"} only. Predictor rows are scaled by their
standard deviation across all units before optimisation, matching the
Synth reference implementation (ADH 2011, JSS); pass
\code{scale_predictors = FALSE} to disable.}

\item{covariates}{An optional named \code{list} of additional time-varying
covariates to partial out before estimation. Each element is a character
string naming a column in \code{data}. Supported for \code{method = "sdid"},
\code{"scm"}, and \code{"gsc"}.}

\item{v_selection}{V matrix selection method for \code{method = "scm"}.
\code{"insample"} (default) follows Abadie et al. (2010): V is chosen by
minimising in-sample pre-treatment MSPE. \code{"oos"} follows Abadie (2021)
S.3.2 / ADH (2015): the pre-treatment window is split into a training
half and a validation half. In the default outcomes-as-predictors case,
candidate W(V) are fitted on training-half outcomes only, V* minimises
the validation-half MSPE, and the final W* is refit with V* on the
outcomes of the last \code{floor(T_pre/2)} pre-treatment periods (so
\code{v_weights} has \code{floor(T_pre/2)} entries). With user-supplied
\code{predictors}, the predictor matrix is fixed and only the MSPE evaluation
window is restricted to the validation half; lag your \code{\link[=pred]{pred()}} windows
to the training period for a fully out-of-sample exercise.}

\item{donor_mspe_threshold}{Donor pool filtering threshold (Abadie 2021 S.4).
For \code{method = "scm"} only. Each donor's individual pre-treatment MSPE
(using that donor alone as the counterfactual) is divided by the minimum
such MSPE across all donors. Donors whose ratio exceeds this threshold are
excluded from estimation. \code{Inf} (default) disables filtering.}

\item{lambda_pen}{Penalised SCM parameter (Abadie & L'Hour 2021, JASA).
For \code{method = "scm"} only. \code{NULL} (default) runs standard unpenalised SCM.
\code{"auto"} selects the penalty via out-of-sample pre-treatment MSPE on the
same validation window as \code{v_selection = "oos"}. A non-negative number
uses that value directly.}

\item{v_optim}{Outer V-optimisation method for \code{method = "scm"}.
The outer problem (choose V so that the implied donor weights W(V)
minimise pre-treatment outcome MSPE) is non-convex, and a single local
search can settle in a poor basin when \code{predictors} are supplied.
\code{"auto"} (default) therefore selects \code{"multistart"} for predictor-based
fits and \code{"coord_descent"} for outcomes-only fits (where the single
start is empirically reliable). \code{"multistart"} runs a deterministic
multi-start search: a fixed start set (uniform V, one-hot V per
predictor, and 100 fixed-seed random draws) is screened at one inner QP
each, the leaders are polished by coordinate descent, and the winner is
refined by a Nelder-Mead pass. Its solution is never worse (in
pre-treatment loss) than \code{"coord_descent"}, at roughly the cost of a
handful of single-start fits, and is fully reproducible (no RNG state
is consumed). \code{"coord_descent"} is the classic single-start coordinate
descent with an 11-point grid, and is also the outcomes-only and
staggered engine and the multi-start never-worse reference.
\code{"bfgs"} (a single-start L-BFGS-B) is \strong{deprecated} and will be removed
in a future release: it has no advantage over \code{"multistart"} (which
dominates it on predictor fits) or \code{"coord_descent"}.
\code{\link[=mspe_ratio_pval]{mspe_ratio_pval()}} mirrors a multi-start fit in its placebo refits so
the permutation test stays symmetric.}

\item{v_window}{Optional vector of pre-treatment time values (matching the
time index in \code{data}) over which the outer V optimisation evaluates the
pre-treatment fit, for \code{method = "scm"} (sharp fits only). \code{NULL}
(default) evaluates on all pre-treatment periods. The window restricts
only the outer evaluation loss: predictor matrices (or the full
pre-treatment outcome rows in the outcomes-only case) still enter the
inner QP unchanged, and the reported \code{loss} and \code{\link[=mspe_ratio_pval]{mspe_ratio_pval()}}
MSPE components always cover the full pre-treatment window. Cannot be
combined with \code{v_selection = "oos"}, which manages its own
train/validation split.}

\item{nu}{Partial pooling parameter for \strong{staggered} SCM fits
(Ben-Michael, Feller & Rothstein 2022, JRSS-B). \code{NULL} (default) keeps
the per-cohort V-optimised SCM path. A number in \verb{[0, 1]} switches to
partially pooled SCM: all cohort weight vectors are chosen jointly to
minimise \verb{nu * (normalised pooled pre-treatment imbalance)^2 + (1 - nu) * (normalised per-cohort imbalance)^2}, so the aggregate ATT
is anchored by the pooled fit. \code{nu = 0} is separate per-cohort SCM
with uniform lag weights, \code{nu = 1} fully pooled, and \code{nu = "auto"}
uses the paper's heuristic (the ratio of the pooled to the average
per-cohort imbalance of the separate solution). The pooled path is
outcomes-only and cannot be combined with \code{donor_mspe_threshold},
\code{lambda_pen}, or \code{v_selection = "oos"}. Balance diagnostics are stored
in \code{fit$pooling}. For \code{method = "scm"} on staggered panels only.}

\item{fixedeff}{If \code{TRUE}, staggered SCM demeans every unit by its own
pre-treatment mean within each cohort before fitting (intercept shift;
Ben-Michael, Feller & Rothstein 2022, Section 5.1), which turns the
estimator into a weighted difference-in-differences and typically
improves fit when outcome levels differ across units. The reported
\code{Y_synth} is shifted back to the raw outcome scale. Works with both
the default and the partially pooled path. For \code{method = "scm"} on
staggered panels only. Default \code{FALSE}.}

\item{...}{Additional arguments forwarded to the specific method
(e.g. \code{r}, \code{lambda}, \code{zeta2}).}
}
\value{
An object of classes \code{c("coresynth_<method>", "coresynth")}.
Fits with staggered adoption additionally inherit from
\code{"coresynth_staggered"}, and multi-arm SI fits from
\code{"coresynth_multiarm"}; S3 methods such as \code{\link[generics:tidy]{tidy()}} and \code{\link[generics:augment]{augment()}}
dispatch on these subclasses.
All methods return at minimum:
\itemize{
\item \code{method}: estimator name
\item \code{estimate}: average treatment effect (ATT)
\item \code{times}: time index vector
\item \code{T_pre}: number of pre-treatment periods
\item \code{Y_treat}: treated unit outcome series
\item \code{gap}: treatment effect series (Y_treat - counterfactual)
}
}
\description{
Unified formula interface for Synthetic Control and related causal
inference methods.  The formula syntax is:
}
\details{
\code{outcome ~ treatment | unit_id + time_id}
}
\examples{
# Synthetic balanced panel: 10 units over 20 periods, unit 1 treated
# after period 15.
set.seed(1)
panel <- expand.grid(unit = 1:10, year = 1:20)
panel$treated <- as.integer(panel$unit == 1 & panel$year > 15)
panel$gdp <- panel$unit + 0.5 * panel$year +
  rnorm(nrow(panel)) + 3 * panel$treated

fit <- scm_fit(gdp ~ treated | unit + year, data = panel, method = "sdid")
summary(fit)

\donttest{
# Visualise the estimated gap (requires ggplot2)
plot(fit, type = "gap")
}
}
