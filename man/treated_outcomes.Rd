% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{treated_outcomes}
\alias{treated_outcomes}
\alias{treated_outcomes.coresynth}
\alias{synthetic_outcomes}
\alias{synthetic_outcomes.coresynth}
\alias{donor_outcomes}
\alias{donor_outcomes.coresynth_scm}
\alias{donor_outcomes.coresynth_sdid}
\alias{donor_outcomes.coresynth_si}
\alias{donor_outcomes.coresynth_gsc}
\alias{donor_outcomes.coresynth_mc}
\alias{donor_outcomes.coresynth_tasc}
\alias{donor_outcomes.coresynth}
\title{Extract Outcome Series from a coresynth Fit}
\usage{
treated_outcomes(x, ...)

\method{treated_outcomes}{coresynth}(x, na.rm = FALSE, ...)

synthetic_outcomes(x, ...)

\method{synthetic_outcomes}{coresynth}(x, na.rm = FALSE, ...)

donor_outcomes(x, ...)

\method{donor_outcomes}{coresynth_scm}(x, ...)

\method{donor_outcomes}{coresynth_sdid}(x, ...)

\method{donor_outcomes}{coresynth_si}(x, ...)

\method{donor_outcomes}{coresynth_gsc}(x, ...)

\method{donor_outcomes}{coresynth_mc}(x, ...)

\method{donor_outcomes}{coresynth_tasc}(x, ...)

\method{donor_outcomes}{coresynth}(x, ...)
}
\arguments{
\item{x}{A \code{coresynth} object from \code{\link[=scm_fit]{scm_fit()}}.}

\item{...}{Passed to methods.}

\item{na.rm}{Logical; passed to the per-period averaging over multiple
treated units (default \code{FALSE}).}
}
\value{
For \code{treated_outcomes()} and \code{synthetic_outcomes()}, a numeric
vector of length \eqn{T}, or \code{NULL}. For \code{donor_outcomes()}, a
\eqn{T \times N_{co}} numeric matrix (donors in columns, named when unit
names are available), or \code{NULL}.
}
\description{
Accessor generics that return the outcome series stored in a fitted
\code{coresynth} object under a uniform interface, regardless of the estimation
method:
}
\details{
\itemize{
\item \code{treated_outcomes()}: the treated unit's observed outcome series
(length \eqn{T}). When several units are treated, their per-period mean.
\item \code{synthetic_outcomes()}: the estimated counterfactual series
(length \eqn{T}), i.e. the synthetic control or model-fitted outcome.
\item \code{donor_outcomes()}: the \eqn{T \times N_{co}} matrix of observed donor
(control unit) outcomes over all periods.
}

Each accessor returns \code{NULL} when the requested series is not stored in
the fit. In particular, staggered-adoption fits keep their data per cohort
(in \code{fit$cohort_fits}), so the sharp-fit accessors return \code{NULL} for them.
}
\examples{
set.seed(1)
panel <- expand.grid(unit = 1:10, year = 1:20)
panel$treated <- as.integer(panel$unit == 1 & panel$year > 15)
panel$gdp <- panel$unit + 0.5 * panel$year +
  rnorm(nrow(panel)) + 3 * panel$treated
fit <- scm_fit(gdp ~ treated | unit + year, data = panel, method = "scm")

y1   <- treated_outcomes(fit)    # observed treated series
y1_0 <- synthetic_outcomes(fit)  # synthetic counterfactual
Yco  <- donor_outcomes(fit)      # donor outcome matrix
all.equal(y1 - y1_0, unname(fit$gap))
}
