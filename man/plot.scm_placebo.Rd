% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.scm_placebo}
\alias{plot.scm_placebo}
\title{Plot SCM In-Space Placebo Results}
\usage{
\method{plot}{scm_placebo}(
  x,
  type = c("gaps", "ratios"),
  mspe_prune = Inf,
  colors = NULL,
  labels = NULL,
  vline = list(),
  vline_offset = 0,
  hline = list(),
  ...
)
}
\arguments{
\item{x}{A \code{scm_placebo} object from \code{\link[=mspe_ratio_pval]{mspe_ratio_pval()}}.}

\item{type}{One of \code{"gaps"} (ADH 2010, Figures 4-7) or \code{"ratios"}
(ADH 2010, Figure 8).}

\item{mspe_prune}{Only for \code{type = "gaps"}: exclude placebo units whose
pre-treatment MSPE exceeds \code{mspe_prune} times the treated unit's.
Default \code{Inf} (no pruning). A rule stated on the RMSPE scale, such as
tidysynth's "2 times the treated unit's pre-period RMSPE", corresponds
to the squared multiple (\code{mspe_prune = 4}).}

\item{colors}{A named vector overriding series colors, e.g.
\code{c(treated = "black")}. Valid keys: \code{"treated"}, \code{"placebo"}.}

\item{labels}{A named vector overriding the legend text of individual
series, e.g. \code{c(treated = "California")}. Valid keys: \code{"treated"},
\code{"placebo"}. Series not mentioned keep their default label; \code{colors}
and \code{labels} address series by the same keys, independent of the
displayed legend text.}

\item{vline}{Only for \code{type = "gaps"}: aesthetic overrides for the vertical
treatment-time line, as a list passed to \code{\link[ggplot2:geom_vline]{ggplot2::geom_vline()}}.
\code{NULL} or \code{FALSE} hides the line entirely. The list may also carry an
\code{xintercept} element giving one or more absolute positions on the time
axis, replacing the default treatment-time position.}

\item{vline_offset}{Only for \code{type = "gaps"}: where to draw the vertical
treatment line, in periods relative to the first post-treatment period.
The default \code{0} keeps the line at the first post-treatment period; \code{-1}
moves it to the last pre-treatment period, and fractional values
interpolate between adjacent observed times. Cannot be combined with an
\code{xintercept} element in \code{vline}.}

\item{hline}{Only for \code{type = "gaps"}: aesthetic overrides for the
horizontal zero line, as a list passed to \code{\link[ggplot2:geom_hline]{ggplot2::geom_hline()}}.
\code{NULL} or \code{FALSE} hides the line entirely.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot2} plot object.
}
\description{
Visualizes the placebo study returned by \code{\link[=mspe_ratio_pval]{mspe_ratio_pval()}}, following
Abadie, Diamond & Hainmueller (2010, Section 3.4).
}
\details{
\code{type = "gaps"} overlays the treated unit's gap path (treated minus
synthetic control) on the placebo gap paths obtained by reassigning the
intervention to each donor unit (ADH 2010, Figure 4). Placebo units whose
synthetic control fits poorly before treatment carry no information about
the rarity of a large post-treatment gap, so ADH exclude units whose
pre-treatment MSPE exceeds a multiple of the treated unit's: 20, 5, and 2
in their Figures 5-7 (\code{mspe_prune}).

\code{type = "ratios"} shows the post/pre-treatment MSPE ratio of every unit
(ADH 2010, Figure 8), the statistic behind the two-sided permutation
p-value; it requires no pruning cutoff by construction.
}
\examples{
set.seed(1)
panel <- expand.grid(unit = 1:10, year = 1:20)
panel$treated <- as.integer(panel$unit == 5 & panel$year > 15)
panel$gdp <- panel$unit + 0.5 * panel$year +
  rnorm(nrow(panel)) + 3 * panel$treated
fit <- scm_fit(gdp ~ treated | unit + year, data = panel, method = "scm")
placebo <- mspe_ratio_pval(fit)

\donttest{
# Treated gap overlaid on the donor-pool placebo gaps (ADH 2010, Fig. 4)
plot(placebo, type = "gaps")

# Prune poorly fitting placebos and relabel the legend
plot(placebo, type = "gaps", mspe_prune = 5,
     labels = c(treated = "Unit 5"))

# Move the treatment line one period earlier
plot(placebo, type = "gaps", vline_offset = -1)

# Post/pre-treatment MSPE ratios (ADH 2010, Fig. 8)
plot(placebo, type = "ratios")
}
}
\seealso{
\code{\link[=mspe_ratio_pval]{mspe_ratio_pval()}}
}
