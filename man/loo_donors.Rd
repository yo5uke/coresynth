% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scm.R
\name{loo_donors}
\alias{loo_donors}
\title{Leave-One-Out Donor Robustness for SCM}
\usage{
loo_donors(fit, weight_threshold = 1e-06)
}
\arguments{
\item{fit}{A sharp \code{coresynth} object from \code{\link[=scm_fit]{scm_fit()}} with
\code{method = "scm"}.}

\item{weight_threshold}{Only donors whose baseline weight exceeds this
value are dropped (removing a zero-weight donor cannot change the fit).
Default \code{1e-6}.}
}
\value{
A list with:
\itemize{
\item \code{att_original}: baseline ATT
\item \code{results}: data.frame with one row per excluded donor
(\code{donor}, \code{weight}, \code{att_loo})
\item \code{att_range}: range of the leave-one-out ATTs
}
}
\description{
Iteratively re-estimates the synthetic control excluding one contributing
donor at a time, holding the predictor weights V fixed at their baseline
values (Abadie, Diamond & Hainmueller 2015, footnote 20). The spread of
the leave-one-out ATT estimates shows how much the result hinges on any
single donor.
}
\details{
For penalised fits (\code{lambda_pen} used), the same penalty is re-applied in
each leave-one-out QP.
}
\seealso{
\code{\link[=placebo_in_time]{placebo_in_time()}}, \code{\link[=mspe_ratio_pval]{mspe_ratio_pval()}}
}
