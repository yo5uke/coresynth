% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_data}
\alias{plot_data}
\alias{plot_data.default}
\alias{plot_data.coresynth}
\alias{plot_data.scm_placebo}
\title{Extract the tidy data behind a coresynth plot}
\usage{
plot_data(x, ...)

\method{plot_data}{default}(x, ...)

\method{plot_data}{coresynth}(
  x,
  type = c("trend", "gap", "weights", "pred_weights"),
  align = FALSE,
  top_n = Inf,
  show_donors = 0,
  ...
)

\method{plot_data}{scm_placebo}(x, type = c("gaps", "ratios"), mspe_prune = Inf, ...)
}
\arguments{
\item{x}{A \code{coresynth} fit (from \code{\link[=scm_fit]{scm_fit()}}) or a \code{scm_placebo} object
(from \code{\link[=mspe_ratio_pval]{mspe_ratio_pval()}}).}

\item{...}{Passed to methods (unused by the current methods).}

\item{type}{For a \code{coresynth} fit: one of \code{"trend"}, \code{"gap"}, \code{"weights"},
or \code{"pred_weights"}, matching \code{\link[=plot.coresynth]{plot.coresynth()}}. For a \code{scm_placebo}
object: \code{"gaps"} or \code{"ratios"}, matching \code{\link[=plot.scm_placebo]{plot.scm_placebo()}}.}

\item{align}{For \code{type = "trend"}/\code{"gap"}: when \code{TRUE}, shift the synthetic
series by its pre-treatment level gap to the treated series, exactly as in
\code{\link[=plot.coresynth]{plot.coresynth()}}. Default \code{FALSE} (raw series).}

\item{top_n}{For \code{type = "weights"}/\code{"pred_weights"}: keep only the \code{top_n}
largest weights (default \code{Inf}, every row).}

\item{show_donors}{For \code{type = "trend"}: also return the outcome paths of the
\code{show_donors} largest-weight donors as \code{series = "Donors"} rows, adding a
\code{unit} column that identifies each donor (\code{NA} for the treated and
synthetic series). Default \code{0} (treated and synthetic series only).}

\item{mspe_prune}{For a \code{scm_placebo} object with \code{type = "gaps"}: drop
placebo units whose pre-treatment MSPE exceeds \code{mspe_prune} times the
treated unit's, as in \code{\link[=plot.scm_placebo]{plot.scm_placebo()}}. Default \code{Inf} (no pruning).}
}
\value{
A tidy \code{data.frame}. Columns by \code{type}:
\describe{
\item{\code{"trend"}}{\code{time}, \code{value}, \code{series} (\code{"Treated"} /
\code{"Synthetic Control"}); with \code{show_donors > 0}, also \code{"Donors"} rows
and a \code{unit} column.}
\item{\code{"gap"}}{\code{time}, \code{gap} (treated minus synthetic control).}
\item{\code{"weights"}}{\code{unit}, \code{weight}; SDID fits add a \code{panel} column
(\code{"omega"} unit weights, \code{"lambda"} time weights), with \code{unit} holding
the pre-period label for \code{"lambda"} rows.}
\item{\code{"pred_weights"}}{\code{predictor}, \code{weight} (sharp SCM only).}
\item{\code{"gaps"}}{\code{time}, \code{gap}, \code{unit} (\code{NA} for the treated series),
\code{series} (\code{"Treated"} / \code{"Placebo (donor pool)"}).}
\item{\code{"ratios"}}{\code{unit}, \code{ratio}, \code{series}.}
}
}
\description{
Returns the tidy \code{data.frame} that \code{\link[=plot]{plot()}} draws for a given \code{type}, so the
underlying series, weights, or placebo paths can be inspected, joined into a
table, or re-plotted directly. \code{\link[=plot]{plot()}} stays the quick path; \code{plot_data()}
is the handle for anyone who wants to relabel simplified series names, feed
the numbers into their own figure, or postprocess them further.
}
\details{
The frame mirrors what the matching \code{plot(x, type = ...)} call shows, with
two deliberate departures that make it a better data source:
\itemize{
\item Plain column names (\code{time}, \code{value}, \code{series}, \code{weight}, ...) are used
instead of the dotted convention of \code{\link[generics:augment]{augment()}}, since this is data to
manipulate rather than model-augmented observations.
\item The cosmetic "drop donors with weight below 1e-4" filter that
\code{plot(type = "weights")} applies is \emph{not} used here: every donor is
returned (use \code{top_n} to subset), so the frame is the complete set of
weights.
}

Only the arguments that change \emph{which rows or values} appear are accepted
(\code{align}, \code{top_n}, \code{show_donors}, \code{mspe_prune}); purely cosmetic arguments
of \code{\link[=plot]{plot()}} (\code{colors}, \code{labels}, \code{vline}, \code{fill}, ...) have no data
counterpart and are not part of this interface.
}
\examples{
set.seed(1)
panel <- expand.grid(unit = 1:10, year = 1:20)
panel$treated <- as.integer(panel$unit == 5 & panel$year > 15)
panel$gdp <- panel$unit + 0.5 * panel$year +
  rnorm(nrow(panel)) + 3 * panel$treated
fit <- scm_fit(gdp ~ treated | unit + year, data = panel, method = "scm")

head(plot_data(fit, type = "trend"))
plot_data(fit, type = "gap")
plot_data(fit, type = "weights")

# Relabel the simplified series names, then plot it yourself
df <- plot_data(fit, type = "trend")
df$series <- sub("Synthetic Control", "Synthetic Unit 5", df$series)
\donttest{
ggplot2::ggplot(df, ggplot2::aes(time, value, color = series)) +
  ggplot2::geom_line()
}
}
\seealso{
\code{\link[=plot.coresynth]{plot.coresynth()}}, \code{\link[=plot.scm_placebo]{plot.scm_placebo()}}
}
