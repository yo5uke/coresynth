% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{scm_placebo_x_cpp}
\alias{scm_placebo_x_cpp}
\title{Fast Leave-One-Out Placebo Test for SCM with a Predictor Specification}
\usage{
scm_placebo_x_cpp(
  X0,
  Y_pre,
  Y_post,
  max_iter = 100L,
  tol = 1e-04,
  z_rows = NULL,
  multistart = FALSE
)
}
\arguments{
\item{X0}{Predictor matrix for control units (k x N_co), on the same
scale as the treated fit (SD-scaled when \code{scale_predictors = TRUE})}

\item{Y_pre}{Control pre-treatment outcomes (T_pre x N_co)}

\item{Y_post}{Control post-treatment outcomes (T_post x N_co)}

\item{max_iter}{Outer coordinate-descent iterations (default 100)}

\item{tol}{Convergence tolerance for V updates (default 1e-4)}

\item{z_rows}{Optional 1-based pre-period row indices of the outer
evaluation window (the \code{v_window} of the treated fit), so each placebo
refit optimises V on the same window. \code{NULL} (default) uses all rows.
MSPE components are always computed on the full pre/post windows.}

\item{multistart}{If \code{TRUE}, each placebo refit uses the same
deterministic multi-start outer search as the treated fit, keeping the
permutation test symmetric.}
}
\value{
A list with:
\itemize{
\item \code{mspe_pre}:  N_co-vector of pre-treatment MSPE per placebo unit
\item \code{mspe_post}: N_co-vector of post-treatment MSPE per placebo unit
\item \code{effects}:   N_co-vector of mean post-period gap per placebo unit
\item \code{gaps}:      (T_pre + T_post) x N_co matrix of placebo gap paths
A placebo unit whose solver fails yields NaN entries.
}
}
\description{
Covariate-spec counterpart of \code{\link[=scm_placebo_cpp]{scm_placebo_cpp()}}: for each control unit,
treats it as pseudo-treated with its own predictor column \code{X0[, i]} and
fits the nested V/W optimisation against the remaining donors' predictors
\code{X0[, -i]}, evaluating the prediction loss on pre-treatment outcomes.
Each leave-one-out problem is identical to a \code{\link[=scm_weights_cpp]{scm_weights_cpp()}} call on
the same submatrices; iterations are independent and run in parallel
under OpenMP.
}
