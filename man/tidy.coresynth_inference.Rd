% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{tidy.coresynth_inference}
\alias{tidy.coresynth_inference}
\title{Tidy an inference result}
\usage{
\method{tidy}{coresynth_inference}(x, conf.int = TRUE, ...)
}
\arguments{
\item{x}{A \code{coresynth_inference} (or \code{sdid_inference}) object returned by
\code{\link[=sdid_inference]{sdid_inference()}}, \code{\link[=gsc_inference]{gsc_inference()}}, or \code{\link[=si_inference]{si_inference()}}.}

\item{conf.int}{Logical. Include \code{conf.low}/\code{conf.high} columns when CI is
available (default \code{TRUE}). Permutation placebo SE/CI are \code{NA}.}

\item{...}{Unused.}
}
\value{
A one-row \code{data.frame} with columns \code{term}, \code{estimate}, \code{std.error},
\code{statistic}, \code{p.value}, \code{conf.low}, \code{conf.high}, \code{method}, \code{alternative},
\code{n_controls}, \code{staggered}.
}
\description{
Coerces a \code{coresynth_inference} or \code{sdid_inference} object to a one-row
tidy \code{data.frame} with broom-style column names so it can be combined
with regression output for paper tables.
}
